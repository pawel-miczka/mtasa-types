package mtasa.shared.enums;

enum abstract BasicElementType(String) {
	var PLAYER = "player";
	var PED = "ped";
	var VEHICLE = "vehicle";
	var OBJECT = "object";
	var PICKUP = "pickup";
	var MARKER = "marker";
}
