package mtasa.shared.enums;

enum abstract LatentEventStatus(String) {
	var TICK_START = 'tickStart';
	var TICK_END = 'tickEnd';
	var TOTAL_SIZE = 'totalSize';
	var PERCENT_COMPLETE = 'percentComplete';
}
